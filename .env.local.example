# ─────────────────────────────────────────────────────────────
# EcoCollect – Biến môi trường (Copy thành .env.local và điền giá trị)
# ─────────────────────────────────────────────────────────────

# URL gốc của Backend (KHÔNG kèm trailing slash, KHÔNG kèm /api)
# Ví dụ: http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
